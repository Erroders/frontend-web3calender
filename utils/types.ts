export type PushNotificationFeed = {
  app: string;
  blockchain: string;
  cta: string;
  icon: string;
  image: string;
  message: string;
  notification: {
    body: string;
    title: string;
  };
  sid: string;
  title: string;
  url: string;
};
